{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Fenêtre principale : commandes Ramure (lecture seule), titre de la fenêtre, dialogue d'ouverture de dossier, mise à jour de l'application et relance après installation.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "dialog:allow-open",
    "updater:default",
    "process:allow-restart"
  ]
}
